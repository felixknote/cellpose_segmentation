# Cellpose segmentation & morphology pipeline — dependencies
#
# Pinned to the versions in the `cellposeSAM` conda env this pipeline runs on
# (Python 3.8.20). Lower bounds are used rather than exact pins so the scripts
# stay installable on newer interpreters; the ==-style versions in comments are
# what has actually been validated.
#
#   conda create -n cellposeSAM python=3.8
#   conda activate cellposeSAM
#   pip install -r requirements.txt
#
# GPU note: `torch` below is the CPU wheel. For CUDA (required for practical
# CellposeSAM throughput) install the matching build first, e.g.
#   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121

# ── Segmentation (01) ─────────────────────────────────────────────────────────
cellpose==4.0.6           # CellposeSAM ("cpsam") weights; API differs across majors
torch>=2.4.0              # validated: 2.4.0
tifffile>=2023.4.12
scikit-image>=0.19.3      # regionprops_table

# ── Data handling (all steps) ─────────────────────────────────────────────────
numpy>=1.24.3,<2          # validated: 1.24.3
pandas>=2.0.3             # validated: 2.0.3
pyarrow>=17.0.0           # parquet shards + combined tables
tqdm>=4.66.5

# ── Analysis & statistics (02, 04) ────────────────────────────────────────────
scipy>=1.10.1
scikit-learn>=1.3.0       # PCA, QuantileTransformer (03, 05)
openpyxl>=3.1.5           # DataFrame.to_excel in 02

# ── Plotting (02, 03, 04) ─────────────────────────────────────────────────────
matplotlib>=3.7.2
seaborn>=0.11.0

# ── Dimensionality reduction (03) ─────────────────────────────────────────────
umap-learn>=0.5.7

# ── Optional ──────────────────────────────────────────────────────────────────
# bokeh powers the interactive HTML scatter plots in 03. The script degrades
# gracefully and skips HTML output when it is absent.
bokeh>=2.4.3
